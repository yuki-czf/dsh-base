# 决策日志 DECISIONS

> 只追加，永不改写历史条目。多会话并发追加是安全的。最新的在最下面。
> 编号格式：#D{N}，递增分配。可选字段 `迭代自` 标注演化关系。
> 超过 200 行时跑 `save.ps1 trim-decisions` 归档旧段。

## [{DATE}] #D1 {决策标题}

- **背景**：{为什么需要做这个决定}
- **决定**：{选了什么}
- **理由**：{为什么选它，权衡了什么替代方案}
- **影响**：{波及哪些模块 / 节点}
