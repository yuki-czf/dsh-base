# {节点名} 批次计划

> 单节点预判超上下文预算时使用：拆批执行，一批 = 一个新会话。纪律全文见 `.nodes/PROTOCOL.md`「批次协议」节；本文件是该节点的执行台账，批末必须回填校准日志。
> 创建：{DATE}

- **拆批确认**：立节点时已按询问制向用户确认走本分批约定（而非 spec-superflow，二选一不混用）
- **PROGRESS 登记**：节点 #{N}，状态 进行中·批1/M（{DATE} 登记，与 PROGRESS.md 该行一致）
- **阈值**：单批材料 ≤30K tokens（校准后在此更新）
- **会话标识**：批 N 用 `<节点>-b<N>`（如 `v130-b1`），与 SESSIONS/main 区分
- **校准日志**：
  - {日期}：批{N} 实际{压缩点/耗时/结论}

| 批 | 名称 | 范围（文件/函数） | 验收标准 | 预估材料K | 前置批 |
|---|------|------------------|----------|-----------|--------|
| 1 | {动宾短语，如「XX 条款起草」} | {本批要读/改哪些文件、函数} | {怎样算本批完成，逐条可勾验} | {预计读入上下文的输入材料大小，K} | - |
| 2 | {…} | {…} | {…} | {…} | {前置批号，如 1} |

> 批末收口固定走 `save.ps1 commit`（lock→CONTEXT→unlock→verify-batch 单命令原子完成）；「已完成批N」字样在 commit 全过（exit 0）之前禁止写入存档或口头宣布（完成字样时序）。

## 批间衔接说明

- {批间依赖与衔接点：批N 产出 = 批N+1 的输入；只消费不回改的边界}
- {批末暂停点固定格式：「已完成批N：<一句话产出>；批N+1 从 <文件/步骤> 开始」}
