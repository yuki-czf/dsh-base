# 节点归档：v1.3.1 原子收口

- **周期**：2026-08-28（单日 4 批：v131-b1 由 main 代行，b2→b3 链式自动派生，b3→b4 断链由 main 补派）
- **验收结果**：全过——①commit 三场景实测（缺参 throw / 正常 exit 0 / 锁竞争 exit 1 暂存保留 / verify-batch FAIL exit 1 CONTEXT 无损）+ 缺参负例；②verify-batch 特有项 C（CONTEXT 时效）+ UTF-8 降噪，旧视角复测精准抓时序差；③询问制六处落盘（plans/README、母版 PROTOCOL/SKILL/BATCH-PLAN/AGENTS、实例计划）；④母版模板 {{BATCH_MODEL}} 占位 + install -BatchAgent 两态独立复测（重写/保留+⚠提示）；⑤镜像 hash diff=0（11=11）；⑥dist v1.3.1.zip（37,564B）sha256 复算一致
- **改动了什么**：
  - `skill\scripts\save.ps1`：新增 `commit` 动作（lock→CONTEXT 原子替换→删暂存→unlock→verify-batch 透传退码）；verify-batch 块函数化为 Invoke-VerifyBatch；特有项 C 盲区校验；头部 UTF-8 console 降噪（批1/批2）
  - 全局 `~/.config/opencode/agents/batch-executor.md`：纪律6 改 commit 单命令收口、纪律7 完成字样时序（verify 全过才写"完成"）（批3）
  - `bridge\opencode\agents\batch-executor.md`：新建，{{BATCH_MODEL}} 占位模板（批4）
  - `install.ps1`：`-BatchAgent [-BatchModel]` 参数 + 3.6 节安装逻辑（批4）
  - `skill\SKILL.md` 1.3.0→1.3.1；母版 PROTOCOL/SKILL/README/AGENTS/BATCH-PLAN 模板同步（批3/4）
  - `dist\node-architect-v1.3.1.zip` + `.sha256`（批4）
- **踩坑记录**：
  - **子脚本调用陷阱**：`& xxx.ps1` 时子脚本内 throw/exit 会终结外层运行区——故障注入测试必须逐场景独立 bash 调用，不可串在一条命令里
  - **测试脚本自身 bug 污染场景**：用 Resolve-Path 写不存在的锁文件失败 → 场景 B 退化成 A；setup 与断言要分离、临时文件显式 Join-Path 创建
  - **派生步仍是断链高发位**：b3 收口干净但未派生 b4（第 4 次同位事件），main 补派——commit 治了收口尾部，派生步尚无兜底（下迭代候选：session.create 失败重试一次/链监控）
  - **cosmetic 未修**：verify-batch 修复提示"批 + 3 +"字符串拼接毛刺（功能无碍）
- **遗留**：
  - 派生步兜底机制（见上）
  - Qwen3 enable_thinking 兜底维持观察期（格式泄漏持续零复发）
  - 节点 3（L2 桥接）进行中、节点 4（v1.2.0 zip 新机实测）待用户、节点 1/2 待验收手续补办
- **给下个节点的衔接说明**：commit 契约已固化（`commit -Session -Node -Batch -ContextFile`，CONTEXT 经临时文件传入，失败语义三态）；batch-executor 真源=母版 `bridge\opencode\agents\`（本机全局文件为生成物，改后手动同步）；询问制口径=「立节点先问 spec-superflow 还是分批约定，二选一」；分发物 dist\node-architect-v1.3.1.zip 含 commit/盲区校验/agent 模板全套
