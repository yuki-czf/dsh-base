# 节点归档：v1.3.0 批次协议固化

- **周期**：2026-08-27 → 2026-08-28（5 批链式执行：v130-b1 → b2 → b3 → b4 → b5）
- **验收结果**：五条全过——①母版 PROTOCOL「批次协议」节六条纪律全覆盖、目录树含 plans/；②templates/BATCH-PLAN.md 六列字段说明完整；③install 镜像 diff=0（11=11 文件）；④实例 .nodes/PROTOCOL.md 与母版一致（批次节 L53+）；⑤dist\node-architect-v1.3.0.zip（31,264B）+ sha256 独立复算一致
- **改动了什么**：
  - `skill\references\PROTOCOL.md`：新增「批次协议」节 + 目录树 plans/ 行（批1；批3 定稿微调六列口径 + verify-batch 一句）
  - `skill\references\templates\BATCH-PLAN.md`：新建（批1）
  - `skill\scripts\save.ps1`：新增 `verify-batch` 动作 + `-Batch` 参数（批2 提前落地）
  - `skill\SKILL.md`：frontmatter 1.2.0→1.3.0；`AGENTS.md`（母版+仓库根）摘要行（批3）
  - install.ps1 同步 `.agents` 镜像（diff=0）；仓库实例 `.nodes\PROTOCOL.md` 同步（批3）
  - `dist\node-architect-v1.3.0.zip` + `.sha256`（批4，pack.ps1）
  - `.nodes\plans\README.md`：试点经验回灌（批5）
- **踩坑记录**：
  - **全局 spec-superflow 插件注入**：每个会话首条消息被注入大段工作流引导，毒害 27B 小模型（行为失序/工具调用格式泄漏）→ batch-executor 加「指令豁免」纪律压制；根治需挪项目级（遗留）
  - **verify-batch 缺闭合 `}`**：verify 分支被吞进 verify-batch 条件块，静默失效；小模型自救补错位置（EOF），main 定位 154 行真缺口修复
  - **小模型「提前声明完成」**：三代会话都在 SESSIONS 预写"五步存档完成"实未走完 → 验收必须以磁盘为准（已入 CONTEXT 链日志）
  - **收口尾部阵亡模式**：b2/b3/b5 死于 CONTEXT/unlock/verify 多跳序列（仅 b4 幸免）→ v1.3.1 `commit` 原子收口立项
  - **节点名空格**：verify-batch 的 -Node 与 PROGRESS 行为精确子串匹配，须逐字一致（含空格）
  - **.ps1 编码卫生**：母版脚本必须 BOM+CRLF（v130-b3 实测 LF 行尾 + GBK 解码破坏字符串解析）
- **遗留**：
  - batch-executor.md 仍为本机实验件（~/.config/opencode/agents/），未进母版分发包
  - spec-superflow 全局插件未挪项目级（指令豁免为压制非根治）
  - Qwen3 enable_thinking 兜底未启用（格式泄漏已零复发，观察期）
  - save.ps1 输出无 UTF-8 console 降噪（mojibake 持续）
  - verify-batch 盲区：只查 CONTEXT 日期，抓不住内容过时
  - 冒烟临时目录 %TEMP%\opencode\node-architect-v130-smoke 待清理
- **给下个节点的衔接说明**：v1.3.1「原子收口」= ①save.ps1 新增 `commit` 动作（lock+写 CONTEXT+unlock+verify 单命令原子化；设计点：CONTEXT 内容以临时文件传入）②agent 纪律改「verify 全过才写完成字样」③UTF-8 console 降噪 ④verify-batch 补 CONTEXT 批号校验。本节点的链式基础设施（batch-executor 九条纪律、plans 约定、校准日志）全部可直接复用
