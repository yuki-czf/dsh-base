# v1.3.2 DSH 适配 · 批次计划

- **PROGRESS 登记**：节点 #7，状态 进行中·批1/2（2026-08-28 登记）
- **阈值**：单批材料 ≤30K tokens（承 v1.3.0/v1.3.1 校准，维持）
- **校准日志**：
  - 2026-08-28：③模型防线实测结论——persona 全文与 {{model}} 解析均正确入上下文（nemotron-3-ultra-free），但 nemotron **无视软纪律**直接寒暄（同 preset 下 Qwen3.8 的 ② 严格遵守）；复现 v1.1.0 教训「确定性不能依赖模型服从提示词」→ 防线改置**选择时刻**（preset name/描述前置警示）+ persona 校验移首句绝对化（对目标模型加固，对异模型尽力而为）；preset.yml/agent.cordis.yml 已修订。③重测可选（异模型大概率仍无视，属已知接受）
  - （待回填）批1 ④微型真批执行后记
- **会话标识**：批 N 用 `v132-b<N>`；本节点批 1 由 main 代行研发，**DSH 实测环节需用户在 DSH 客户端配合**
- **链式派生**：不适用（DSH 侧无 openchamber；且批 1 实测依赖用户）

| 批 | 名称 | 范围（文件/函数） | 验收标准 | 预估材料K | 前置批 |
|---|------|------------------|----------|-----------|--------|
| 1 | DSH preset 研发 + 实测 | 新建 `~/.dsh/.agent-presets/batch-executor/{agent.cordis.yml,preset.yml}`（格式照 reddit-pg 样例；服务面与纪律见下方「批1 执行材料」）；DSH 侧实测 | ①DSH 挂载接受（preset 会话可选）②dsh-base 读档复述正确 ③模型防线=选择时刻人工确保（name/描述警示）+ persona 加固尽力而为——**软纪律实测仅对目标模型可靠**（nemotron 实测无视，见校准日志）④微型真批跑通 | 10 | - |
| 2 | 母版收编 + install 探测 + 分发 | 母版新建 `bridge/dsh/agent-presets/batch-executor/`（两文件收编）；install.ps1 探测 `~/.dsh` 且 `-BatchAgent` 语义扩展（opencode→项目 agent；DSH→复制 preset 到 `~/.dsh/.agent-presets/`）；bridge README DSH 行 + plans/README 补 DSH 手动链段；SKILL bump 1.3.2；install 同步镜像；pack.ps1 出 v1.3.2 zip；commit 收口 | install 双客户端探测冒烟；镜像 diff=0；dist v1.3.2 zip+sha256；PROGRESS → 待验收 | 20 | 1 |

## 批间衔接说明

- 批1 本机验证通过的 preset 文件 = 批2 收编蓝本（先实证再入库）
- 批2 完成后母版为准、镜像跟进；末批收口后 PROGRESS → 待验收，不派生

## 批1 执行材料（本节点由 main 代行研发；实测需用户在 DSH 配合）

**设计要点（已敲定，勿发散）**：

- **服务面**（照样例，只列启用项）：persona（九条纪律全文）→ agent-instructions（maxBytes 65536）→ tool-pwsh（`disabled: !!js process.platform !== 'win32'`，与 tool-bash 成对镜像样例条件写法，保证母版可移植）→ tool-fs / tool-fs-search / tool-jobs / skill-filesystem / tool-skill / tool-ask-user / tool-todo → compaction 组（compaction-basic + command-compact + tool-result-pruner，threshold 8192/4096/1024 原样）
- **排除项**（禁并行与缩面）：delegation 组（含 tool-subagent*/workflow*/tool-ralph 全家）、planning 组（执行员不做规划）、tool-goal、tool-web、mcp-*（本项目无 MCP）
- **persona 模板校验**（补偿 preset 不可钉模型）：开头即写「你的模型是 {{model}}。执行模型必须是公司 vLLM 的 Qwen3.8-27B-W4A16-AWQ——不符则立即停止并告知用户切换，不得开始任何工作」
- **纪律差异** vs opencode 版：纪律 7 改为「DSH 无链式派生：commit 全过后输出一行总结并注明手动恢复话术，立即停止」；其余 0-6/8/9 等同
- **YAML 注意**：persona 用字面块 `|` 保持换行；包名全部 `@deepseek-ai/dsh-*`（照样例逐字）；group 用 `cordis:group` + `isolate`（compaction 组 isolate 照抄：compaction+toolResultPruner 双 realm）

**输入材料**：reddit-pg 样例（267 行，本机实证可挂载）、全局 batch-executor 终稿（44 行九条纪律）、settings.yaml vLLM 段（model id 确认：Qwen3.8-27B-W4A16-AWQ）

**边界（禁做）**：不动 opencode 侧任何文件；不动全局插件；不发明样例中不存在的 cordis 语法；不做链式派生（二期探明插件 API 再议）

**验收自检**：①两文件 YAML 可解析（字段/包名与样例逐字核对）②挂载后 preset 出现在 DSH 会话可选列表（用户确认）③读档复述正确（用户确认）④换模型拒绝路径（用户确认）⑤微型真批（用户确认；建议=让 preset 会话对本仓库做一个 1 分钟微任务，如「把 plans/README 的 DSH 手动链段落错字检查一遍」）
