# 会话 v8-b1 · 节点 8 批1（web API 反推）

- **状态**：批1 收口进行中，待 verify-batch 确认（2026-08-28）
- **批1 交付**：API 契约笔记已落 `plans/DSH自动派生探路.md`「批1 产出」节。结论=**分支 A 可行**：
  - `POST /api/<method>` 单一 RPC 面（`session.create`/`session.selectModel`/`session.prompt` 三连即派生），完整契约见计划文件
  - 唯一硬闸 = DesktopWebServer browser access（`compatibility`+`openBrowser` 放行，loopback 保持）；renderer token 内存态不可取；第二道信任栅栏对本地进程天然通过
  - GET 实测（零 POST）与代码结论一致；app.asar 解包件在 `%TEMP%\dsh-asar\app`（只读，未动 DSH 本体）
- **校准**：批1 材料 ~85K（超 30K 预算），捷径已记录（直奔 `dsh-host-apiproxy` UNARY_ROUTES）
- **暂停点**：已完成批1：API 契约笔记落 `plans/DSH自动派生探路.md`「批1 产出」节（分支 A，派生三连 + 鉴权两道栅栏 + 放行方式）；批2 从该文件的「批2 分支 A 执行序」节开始（会话 v8-b2，前置=用户开浏览器访问，首个真 POST 前须用户知情）
- **给批2（v8-b2）的衔接**：
  1. 前置=用户在 DSH 设置开 compatibility+浏览器访问（**loopback**）并重启
  2. pwsh 封装派生三连；**首个真 POST 前须用户知情**（边界约定）
  3. 真批末自动派生实测 → 母版 bump v1.3.3 + install 同步 + pack zip
  4. 失败 fallback：cordis 插件通道（plans 批1 产出 §5）
- **未决问题**：无（探路拿到确定性答案）
